# caring_presentation

케어링 인턴 담당 업무 주간보고 — 정적 슬라이드 덱 (HTML/CSS/JS).

- 엔트리: [`260627/index.html`](260627/index.html) (루트 `/`는 `/260627/`로 리다이렉트)
- 미디어(사진·영상): `260627/media/` — 영상은 web용으로 H.264 압축(faststart) 후 리포지토리에 직접 저장
- 배포: Vercel (정적 호스팅)

## 로컬에서 보기
```bash
python3 -m http.server 8000
# http://localhost:8000/  (→ /260627/ 로 이동)
```
